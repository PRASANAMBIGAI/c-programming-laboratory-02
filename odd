#include <stdio.h> 
int main()
{ 
    int n; 
    printf("Enter an integer: "); 
    scanf("%d", &n);   
    if (number & 1)
    { 
        printf("%d is odd.\n", number); 
    }
    else 
    { 
        printf("%d is even.\n", number); 
    } 
    return 0; 
} 
